<p id="notice"><%= notice %></p>

<h1>Account Information</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= @user.['name'] || "#{auth['first_name']} #{auth['last_name']}"%></td>
        <td><%= @user.email %></td>
        <td><%= link_to 'Edit', edit_user_path(@user) %></td>
        <td><%= link_to 'Delete Account', @user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  </tbody>
</table>

<br>

<%= link_to 'Go to My Boards', boards_path %>
